# dependências
node_modules
.pnpm-store

# next
.next
out
next-env.d.ts

# ambiente
# Regra ampla de propósito: qualquer .env fica de fora, e só o exemplo (que tem
# marcadores, nunca segredo de verdade) é liberado. Listar variantes uma a uma é
# como segredo vaza — basta alguém criar um .env.producao que ninguém previu.
.env*
!.env.example

# payload
/media
/public/media

# sistema
.DS_Store
*.log
.vercel
.turbo

# artefatos de build
tsconfig.tsbuildinfo

# gerados pelo Next para agentes de IA
AGENTS.md
CLAUDE.md
